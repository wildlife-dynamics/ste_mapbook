id: mapbook_report 
requirements:
  - name: ecoscope-platform
    version: ">=2.15.0, <2.16.0"
    channel: https://repo.prefix.dev/ecoscope-workflows/

  - name: ecoscope-workflows-ext-custom
    version: "0.1.0rc14.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: ecoscope-workflows-ext-ste
    version: "0.0.0rc1.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: pydeck
    version: 0.9.2
    channel: conda-forge

  - name: opentelemetry-sdk
    version: ">=1.20.0,<2.0.0"
    channel: conda-forge

rjsf-overrides:
  properties:
    # Subject Group
    Subject Group.properties.subject_group_var.properties.var.title: Subject Group Name
    Subject Group.properties.subject_group_var.properties.var.default: Elephants
    Subject Group.properties.subject_group_var.properties.var.description: "Enter the subject group name exactly as it appears in EarthRanger (case-sensitive)."

    # Trajectory Segment Filter — cleaner titles and descriptions
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.min_length_meters.title: "Minimum Segment Length (m)"
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.min_length_meters.description: "Discard segments shorter than this distance. Helps remove GPS noise and stationary fixes."
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.max_length_meters.title: "Maximum Segment Length (m)"
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.max_length_meters.description: "Discard segments longer than this distance. Helps remove unrealistic long-distance jumps."
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.min_time_secs.title: "Minimum Segment Duration (s)"
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.min_time_secs.description: "Discard segments with a duration shorter than this (in seconds)."
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.max_time_secs.title: "Maximum Segment Duration (s)"
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.max_time_secs.description: "Discard segments with a duration longer than this (in seconds). Default is 6 hours (21,600 s)."
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.min_speed_kmhr.title: "Minimum Speed (km/h)"
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.min_speed_kmhr.description: "Discard segments where average speed falls below this value."
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.max_speed_kmhr.title: "Maximum Speed (km/h)"
    Trajectory Segment Filter.properties.custom_trajs_filter.properties.max_speed_kmhr.description: "Discard segments where average speed exceeds this value. For elephants, 9 km/h is a sensible ceiling."

    # Report Logo (now part of the Generate Word Doc Report task-group)
    Generate Word Doc Report.properties.logo_path.properties.input_method.description: "Provide the organization logo to display on the report cover page (PNG or JPG recommended). Required when 'Generate Word Doc Report' is enabled; leave blank otherwise."

    # Previous Period — clarify that the comparison period ends where the current time range begins
    Previous Period.properties.set_previous_period.properties.offset.description: "Choose how to calculate the comparison period's Start Date. In every mode, the comparison period ends on your selected time range's Start Date (so it never overlaps with the current period) — only the Start Date changes."

    # Mean Speed Raster — clarify what Step Length controls, and label the anyOf branches meaningfully
    Mean Speed Raster.properties.generate_mean_speed_raster.properties.step_length.title: "Step Length (m)"
    Mean Speed Raster.properties.generate_mean_speed_raster.properties.step_length.description: "Sets the pixel (cell) size of the mean speed raster, in meters. Smaller values produce a more detailed raster but take longer to generate and produce larger files; larger values produce a coarser raster. Enter a fixed value (default 2000 m), or choose \"Auto\" to let the system use the average distance between consecutive GPS fixes instead."
    Mean Speed Raster.properties.generate_mean_speed_raster.properties.step_length.anyOf:
      - type: integer
        title: "Fixed value (m)"
      - type: "null"
        title: "Auto (use average step distance)"

  $defs:
    # Restrict category options to Subject Name, Subject Sex and Subject Subtype
    ValueGrouper.properties.index_name.oneOf:
      - const: subject_name
        title: Subject Name

      - const: subject_sex
        title: Subject Sex

      - const: subject_subtype
        title: Subject Subtype

    # Previous Period — Custom: drop Hours/Minutes/Seconds (too fine-grained for this report) and default Months to 1
    CustomPreviousPeriod.description: "Manually set how far back the comparison period's Start Date should be, counted backward from your time range's start date. For example, the default (Months = 1) creates a comparison period covering the month immediately before your report's start date."
    CustomPreviousPeriod.properties:
      years:
        title: Years
        type: integer
        default: 0
        description: "Whole years to go back from your time range's start date."
      months:
        title: Months
        type: integer
        default: 1
        description: "Whole months to go back from your time range's start date."
      weeks:
        title: Weeks
        type: integer
        default: 0
        description: "Whole weeks to go back from your time range's start date."
      days:
        title: Days
        type: integer
        default: 0
        description: "Whole days to go back from your time range's start date."

    # Previous Period — Preset
    PresetPreviousPeriod.properties.preset.description: "Choose a common lookback period, counted backward from your time range's start date. \"Same as current period\" reuses your currently selected Start and End dates unchanged."

    # Previous Period — Calendar
    CalendarPreviousPeriod.properties.since.description: "Pick the exact Start Date for the comparison period."

task-instance-defaults:
  skipif:
    conditions:
      - any_is_empty_df
      - any_dependency_skipped

workflow: 
  - name: Workflow Details
    id: workflow_details
    task: set_workflow_details
  
  - name: Time Range
    id: time_range
    task: set_time_range

  - name: Extract Timezone Selection
    id: get_timezone
    task: get_timezone_from_time_range
    partial:
      time_range: ${{ workflow.time_range.return }}

  - name: Connect to Earth Ranger
    id: er_client_name
    task: set_er_connection 

  - name: Connect to Earth Engine 
    id: gee_project_name
    task: set_gee_connection 

  - name: Configure grouping strategy 
    id: groupers 
    task: set_groupers 
    partial: 
      groupers: 
        - index_name: subject_name 
  
  - name: Configure basemap layers 
    id: configure_base_maps 
    task: set_base_maps_pydeck
    partial:
      base_maps:
        - url: >-
            https://server.arcgisonline.com/arcgis/rest/services/Elevation/World_Hillshade/MapServer/tile/{z}/{y}/{x}
          opacity: 1
          max_zoom: 20
  
  - title: Subject Group
    type: task-group
    description: Choose subject group to analyze 
    tasks:
      - name: null
        id: subject_group_var
        task: set_string_var

  - title: Previous Period
    type: task-group
    description: >-
      Define the comparison ("previous") period to contrast against your selected time range.
      Every option below computes a period that ends on the same End Date as your time range, then
      works backward to determine its Start Date. Choose Custom to enter your own Years / Months /
      Weeks / Days offset (defaults to 1 month back), Preset for a common lookback (e.g. 1, 3 or 6
      months, or 1 year back), or Calendar to pick an exact Start Date.
    tasks: 
      - name: null
        id: set_previous_period
        task: ecoscope_workflows_ext_ste.tasks.filter.flexible_previous_period
        partial: 
          timerange: ${{ workflow.time_range.return }}

  - title: Map Overlay
    type: task-group
    description: "Choose the overlay to display on all maps: (a) LandDx — standard protected areas downloaded automatically; (b) EarthRanger Spatial Feature — fetch a named feature set from EarthRanger."
    tasks:
      - name: null
        id: map_overlay
        task: ecoscope_workflows_ext_ste.tasks.landdx.select_map_overlay # allow user to include multiple ER spatial features
        skipif:
          conditions:
            - never
        partial:
          client: ${{ workflow.er_client_name.return }}
          output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
          ldx_url: "https://www.dropbox.com/scl/fi/uitptfgxk4wnfcnv9k96a/mapbook_ldx_layers.gpkg?rlkey=xi2azbfzqix9udytv3smsf6eh&st=249w3d2x&dl=0"

  - name: Choose subject group to analyze 
    id: subject_observations 
    task: get_subjectgroup_observations 
    partial: 
      filter: clean
      client: ${{ workflow.er_client_name.return }}
      time_range: ${{ workflow.time_range.return }}
      subject_group_name: ${{ workflow.subject_group_var.return }}
      raise_on_empty: false 
      include_details: false 
      include_subjectsource_details: false 

  - name: Convert relocs to timezone
    id: convert_relocs_timezone
    task: convert_values_to_timezone
    partial:
      df: ${{ workflow.subject_observations.return }}
      timezone: ${{ workflow.get_timezone.return }}
      columns:
        - fixtime

  - name: Transform observations to relocations 
    id: subject_reloc
    task: process_relocations 
    partial: 
      observations: ${{ workflow.convert_relocs_timezone.return }}
      relocs_columns: 
        - groupby_col
        - fixtime
        - junk_status 
        - geometry
        - extra__subject__name
        - extra__subject__hex
        - extra__subject__sex
        - extra__created_at
        - extra__subject__subject_subtype

      filter_point_coords:
        - x: 180.000000
          y: 90.000000
        - x: 0.000000
          y: 0.000000
        - x: 1.000000
          y: 1.000000

  - name: Annotate relocations with day/night labels
    id: annotate_day_night 
    task: classify_is_night 
    partial: 
      relocations: ${{ workflow.subject_reloc.return }}

  - title: Trajectory Segment Filter  
    type: task-group
    description: Filter trajectory segments based on criteria like minimum/maximum length, duration and speed. This helps remove GPS noise and unrealistic movements. The same filter will be applied to both current and previous trajectories to ensure consistency in the analysis.
    tasks:
      - name: null
        id: custom_trajs_filter
        task: ecoscope_workflows_ext_ste.tasks.transformation.trajectory_segment_filter
  
  - name: Convert relocations to trajectories 
    id: convert_to_trajectories 
    task: relocations_to_trajectory 
    partial: 
      relocations: ${{ workflow.annotate_day_night.return }}
      trajectory_segment_filter: ${{ workflow.custom_trajs_filter.return }}

  - name: Add temporal index to trajectories 
    id: add_temporal_index_to_traj 
    task: add_temporal_index
    partial: 
      df: ${{ workflow.convert_to_trajectories.return }}
      time_col: segment_start 
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true 
      format: mixed 

  # retrieve previous relocs and trajs based on time range set earlier
  - name: Retrieve previous period observations
    id: previous_observations 
    task: get_subjectgroup_observations 
    partial: 
      filter: clean
      client: ${{ workflow.er_client_name.return }}
      time_range: ${{ workflow.set_previous_period.return }}
      subject_group_name: ${{ workflow.subject_group_var.return }}
      raise_on_empty: false 
      include_details: false 
      include_subjectsource_details: false
  
  - name: Convert previous relocs to timezone
    id: convert_prev_timezone
    task: convert_values_to_timezone
    partial:
      df: ${{ workflow.previous_observations.return }}
      timezone: ${{ workflow.get_timezone.return }}
      columns:
        - fixtime

  - name: Transform previous observations to relocations 
    id: previous_relocs
    task: process_relocations
    partial: 
      observations: ${{ workflow.convert_prev_timezone.return }}
      relocs_columns: 
        - groupby_col
        - fixtime
        - junk_status 
        - geometry
        - extra__subject__name
        - extra__subject__hex
        - extra__subject__sex
        - extra__created_at
        - extra__subject__subject_subtype

      filter_point_coords:
        - x: 180.000000
          y: 90.000000
        - x: 0.000000
          y: 0.000000
        - x: 1.000000
          y: 1.000000

  - name: Annotate previous relocations with day/night labels
    id: annotate_prev_day_night 
    task: classify_is_night 
    partial: 
      relocations: ${{ workflow.previous_relocs.return }}

  - name: Convert  previous relocations to trajectories 
    id: convert_prev_to_trajectories 
    task: relocations_to_trajectory 
    partial: 
      relocations: ${{ workflow.annotate_prev_day_night.return }}
      trajectory_segment_filter: ${{ workflow.custom_trajs_filter.return }}

  - name: Add temporal index to previous trajectories 
    id: add_temporal_prev_index_to_traj 
    task: add_temporal_index
    partial: 
      df: ${{ workflow.convert_prev_to_trajectories.return }}
      time_col: segment_start 
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true 
      format: mixed 

  - name: Rename trajectory columns for previous period
    id: rename_prev_traj_cols
    task: map_columns
    partial:
      raise_if_not_found: false
      df: ${{ workflow.add_temporal_prev_index_to_traj.return }}
      drop_columns: []
      retain_columns: [] 
      rename_columns: 
        extra__hex: hex_color
        extra__is_night: is_night
        extra__name: subject_name
        extra__sex: subject_sex
        extra__subject_subtype: subject_subtype
        extra__created_at: created_at

  - name: Classify trajectories by speed 
    id: classify_trajectories_speed_bins 
    task: apply_classification 
    partial: 
      df: ${{ workflow.add_temporal_index_to_traj.return }}
      input_column_name: speed_kmhr
      output_column_name: speed_bins
      classification_options: 
        scheme: equal_interval 
        k: 6 
      label_options:
        label_ranges: true
        label_decimals: 1
        label_suffix: ' km/h'
  
  - name: Rename trajectory columns 
    id: rename_traj_cols 
    task: map_columns 
    partial:
      raise_if_not_found: true
      df: ${{ workflow.classify_trajectories_speed_bins.return }}
      drop_columns: []
      retain_columns: [] 
      rename_columns: 
        extra__hex: hex_color
        extra__is_night: is_night
        extra__name: subject_name
        extra__sex: subject_sex
        extra__subject_subtype: subject_subtype
        extra__created_at: created_at

  - name: Persist trajectories as geoparquet
    id: persist_trajs_geoparquet
    task: persist_df
    partial:
      df: ${{ workflow.rename_traj_cols.return }}
      filetype: geoparquet
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename: trajectories

  - name: Persist previous period trajectories as geoparquet
    id: persist_prev_trajs_geoparquet
    task: persist_df
    partial:
      df: ${{ workflow.rename_prev_traj_cols.return }}
      filetype: geoparquet
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename: previous_period_trajectories

  - name: Persist relocations as geoparquet
    id: persist_relocs_geoparquet
    task: persist_df
    partial:
      df: ${{ workflow.subject_reloc.return }}
      filetype: geoparquet
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename: relocations

  - name: Persist previous period relocations as geoparquet
    id: persist_prev_relocs_geoparquet
    task: persist_df
    partial:
      df: ${{ workflow.previous_relocs.return }}
      filetype: geoparquet
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename: previous_period_relocations

  - name: Create column for current duration status 
    id: create_current_duration_column 
    task:  ecoscope_workflows_ext_ste.tasks.transformation.add_new_column
    partial:
      df: ${{ workflow.rename_traj_cols.return }}
      column_name: duration_status
      default_value: Current tracks
    
  - name: Create column for previous duration status
    id: create_previous_duration_column 
    task: ecoscope_workflows_ext_ste.tasks.transformation.add_new_column
    partial:
      df: ${{ workflow.rename_prev_traj_cols.return }}
      column_name: duration_status
      default_value: Previous tracks

  - name: Concatenate current and previous trajectories
    id: merge_current_prev_trajs
    task: ecoscope_workflows_ext_ste.tasks.transformation.concatenate_dataframes
    skipif:
      conditions:
        - never
    partial:
      list_df:
        - ${{ workflow.create_current_duration_column.return }}
        - ${{ workflow.create_previous_duration_column.return }}
      
      axis: 0 
      join: outer 
      ignore_index: true 
      keys: null 
      levels: null 
      names: null 
      verify_integrity: false
      sort: true 
      copy: null 

  - name: Add temporal index to combined trajectories
    id: add_temporal_index_to_comb_trajs
    task: add_temporal_index
    partial: 
      df: ${{ workflow.merge_current_prev_trajs.return }}
      time_col: segment_start 
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true 
      format: mixed 
  
  - name: Split trajectories by group 
    id: split_traj_by_group 
    task: split_groups
    partial: 
      df: ${{ workflow.create_current_duration_column.return }}
      groupers: ${{ workflow.groupers.return }}

  - title: Get subject name and use them to persist files with it 
    type: task-group
    description: Choose a grouper to extract subject names for file naming. This will help in organizing the output files based on individual subjects. 
    tasks:
      - name: Get first subject name for filenames
        id: unique_subject_name
        task: column_first_unique_value
        partial:
          column_name: subject_name
        mapvalues:
          argnames: df
          argvalues: ${{ workflow.split_traj_by_group.return }}

      - name: Safe-string subject name for filenames
        id: safe_subject_name
        task: ecoscope_workflows_ext_ste.tasks.transformation.safe_string
        mapvalues:
          argnames: value
          argvalues: ${{ workflow.unique_subject_name.return }}

      - name: Build movement tracks filename 
        id: movement_filename 
        task: prefix_string_var
        partial: 
          var: _movement_tracks.html 
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build speedmap filename 
        id: speedmap_filename 
        task: prefix_string_var
        partial: 
          var: _speedmap.html 
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build day/night filename 
        id: dn_filename 
        task: prefix_string_var
        partial: 
          var: _day_night.html 
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build homerange filename 
        id: hr_filename 
        task: prefix_string_var
        partial: 
          var: _homerange.html 
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build mean speed raster filename 
        id: mr_filename 
        task: prefix_string_var
        partial: 
          var: _mean_speed_raster.html 
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build seasonal homerange filename 
        id: seasonal_hr_filename 
        task: prefix_string_var
        partial: 
          var: _seasonal_homerange.html 
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build ETD filename
        id: etd_filename
        task: prefix_string_var
        partial:
          var: _etd
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build NDVI values filename
        id: ndvi_values_filename
        task: prefix_string_var
        partial:
          var: _ndvi_values
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build MCP filename
        id: mcp_filename
        task: prefix_string_var
        partial:
          var: _mcp
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build seasonal ETD filename
        id: seasonal_etd_filename
        task: prefix_string_var
        partial:
          var: _seasonal_etd
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

      - name: Build mean speed raster filename
        id: mean_speed_raster_filename
        task: prefix_string_var
        partial:
          var: _mean_speed_raster
        mapvalues:
          argnames: prefix 
          argvalues: ${{ workflow.safe_subject_name.return }}

  - name: Split combined trajectories by group
    id: split_comb_trajs 
    task: split_groups
    partial: 
      df: ${{ workflow.add_temporal_index_to_comb_trajs.return }}
      groupers: ${{ workflow.groupers.return }}

  - name: Assign duration status colors 
    id: assign_duration_colors
    task: ecoscope_workflows_ext_ste.tasks.transformation.add_status_color_columns
    partial:
      hex_column: hex_color
      previous_color_hex: "#808080"
      use_hex_column_for_current: true 
      default_current_hex: "#00008b"
      current_status: "Current tracks"
      status_column: "duration_status"
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.split_comb_trajs.return }}

  # 1. Movement tracks 
  - name: Sort trajectories by duration status 
    id: sort_trajs_by_status
    task: sort_values
    partial: 
      column_name: duration_status
      na_position: first 
      ascending: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.assign_duration_colors.return }}

  - name: Exclude unnecessary columns from movement gdf 
    id: filter_movement_cols 
    task: subset_columns 
    partial: 
      columns: 
        - duration_status
        - duration_status_colors
        - is_night
        - geometry
      exclude: null
      strict: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.sort_trajs_by_status.return }}

  - name: Create movement track layers
    id: generate_track_layers
    task: ecoscope_workflows_ext_custom.tasks.results.create_path_layer
    partial: 
      data_url: null
      layer_style:
        get_color: duration_status_colors
        get_width: 2.25
        width_scale: 1 
        width_min_pixels: 2 
        width_max_pixels: 8 
        width_units: pixels
        cap_rounded: true 
        joint_rounded: true
        billboard: false
        opacity: 0.45
        stroked: true 
      legend:
        title: Movement Tracks
        label_column: duration_status
        color_column: duration_status_colors
        sort: ascending 
        label_suffix: null
    mapvalues:
      argnames: geodataframe
      argvalues: ${{ workflow.filter_movement_cols.return }}

  - name: Combine overlay layers and movement track layers
    id: combined_ldx_movement_layers
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers: ${{ workflow.map_overlay.return }}
    mapvalues:
      argnames: grouped_layers
      argvalues: ${{ workflow.generate_track_layers.return }}

  - title: Map Zoom & Extent
    type: task-group
    description: Calculate the geographic extent of the movement tracks and determine an appropriate zoom level and center point for the map. This ensures that all tracks are visible and well-framed when the map is rendered.
    tasks:
      - name: null
        id: zoom_to_envelope 
        task: ecoscope_workflows_ext_ste.tasks.spatial_operations.envelope_gdf
        # partial:
        #   expansion_factor: 1.05
        mapvalues: 
          argnames: gdf 
          argvalues: ${{ workflow.filter_movement_cols.return }}
  
      - name: Zoom view to gdf extent
        id: gdf_image_extent
        task: ecoscope_workflows_ext_ste.tasks.spatial_operations.compute_view_state_from_gdf
        partial:
          pitch: 0
          bearing: 0
          max_zoom: 15
        mapvalues:
          argnames: gdf
          argvalues: ${{ workflow.zoom_to_envelope.return }}
  
  - name: Combine movement track layers with view state
    id: zip_tracks_with_viewstate
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.combined_ldx_movement_layers.return }}
        - ${{ workflow.gdf_image_extent.return }}

  - name: Draw movement tracks map
    id: draw_movement_tracks
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map 
    partial: 
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null 
      max_zoom: 10
      legend_style: 
        placement: bottom-right
    mapvalues:
      argnames: 
        - geo_layers 
        - view_state
      argvalues: ${{ workflow.zip_tracks_with_viewstate.return }}

  - name: Combine movement track filename with text
    id: movement_tracks_text
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.movement_filename.return }}
        - ${{ workflow.draw_movement_tracks.return }}
  
  - name: Persist movement tracks html
    id: persist_movement_tracks_html
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: null
    mapvalues:
      argnames: 
        - filename
        - text 
      argvalues: ${{ workflow.movement_tracks_text.return }}

  - name: Create movement tracks widgets 
    id: create_movement_tracks_widgets 
    task: create_map_widget_single_view 
    skipif:
      conditions:
        - never
    partial: 
      title: Movement Tracks
    map: 
      argnames: 
        - view 
        - data 
      argvalues: ${{ workflow.persist_movement_tracks_html.return }}

  - name: Merge movement tracks widgets 
    id: merge_movement_tracks_widgets 
    task: merge_widget_views 
    partial: 
      widgets: ${{ workflow.create_movement_tracks_widgets.return }}

  #2. Speedmap 
  - name: Sort trajectories by speed bins 
    id: sort_trajs_by_speed 
    task: sort_values
    partial: 
      column_name: speed_bins 
      na_position: first 
      ascending: true 
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.split_traj_by_group.return }}

  - name: Apply colormap to speed bins 
    id: apply_speed_colormap 
    task: apply_color_map 
    partial: 
      input_column_name: speed_bins
      output_column_name: speed_bins_colormap 
      colormap: 
        - '#1a9850'
        - '#91cf60'
        - '#d9ef8b'
        - '#fee08b'
        - '#fc8d59'
        - '#d73027'
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.sort_trajs_by_speed.return }}

  - name: Exclude unnecessary columns from speedmap gdf 
    id: filter_speed_cols 
    task: subset_columns 
    partial: 
      columns: 
        - dist_meters
        - speed_bins_colormap
        - geometry
        - speed_kmhr
        - hex_color
        - speed_bins
      exclude: null
      strict: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.apply_speed_colormap.return }}  

  - name: Generate speedmap layers 
    id: generate_speedmap_layers 
    task: ecoscope_workflows_ext_custom.tasks.results.create_path_layer 
    partial: 
      data_url: null
      layer_style:
        get_color: speed_bins_colormap
        get_width: 2.25
        width_scale: 1 
        width_min_pixels: 2 
        width_max_pixels: 8 
        width_units: pixels
        cap_rounded: true 
        joint_rounded: true
        billboard: false
        opacity: 0.45
        stroked: true 
      legend:
        title: Speed (km/h)
        label_column: speed_bins
        color_column: speed_bins_colormap
        sort: ascending 
        label_suffix: null
    mapvalues:
      argnames: geodataframe
      argvalues: ${{ workflow.filter_speed_cols.return }}

  - name: Combine overlay layers and speedmap layers
    id: combined_ldx_speed_layers
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers: ${{ workflow.map_overlay.return }}
    mapvalues:
      argnames: grouped_layers
      argvalues: ${{ workflow.generate_speedmap_layers.return }}

  - name: Combine speedmap layers with view state 
    id: zip_speedmap_with_viewstate
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.combined_ldx_speed_layers.return }}
        - ${{ workflow.gdf_image_extent.return }}

  - name: Draw speedmap 
    id: draw_speedmap
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map 
    partial: 
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null 
      max_zoom: 10
      legend_style: 
        placement: bottom-right
    mapvalues:
      argnames: 
        - geo_layers 
        - view_state
      argvalues: ${{ workflow.zip_speedmap_with_viewstate.return }}

  - name: Zip speed filename with text 
    id: speedmap_file_text 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.speedmap_filename.return }}
        - ${{ workflow.draw_speedmap.return }}

  - name: Persist speedmap html
    id: persist_speedmap_html
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: null
    mapvalues:
      argnames: 
        - filename
        - text 
      argvalues: ${{ workflow.speedmap_file_text.return }}
    
  - name: Create speedmap widgets 
    id: create_speedmap_widgets 
    task: create_map_widget_single_view 
    skipif:
      conditions:
        - never
    partial: 
      title: Speed Map 
    map: 
      argnames: 
        - view 
        - data 
      argvalues: ${{ workflow.persist_speedmap_html.return }}

  - name: Merge speedmap widgets 
    id: merge_speedmap_widgets 
    task: merge_widget_views 
    partial: 
      widgets: ${{ workflow.create_speedmap_widgets.return }}

  # 3. Day night map 
  - name: Sort trajectories by day/night 
    id: sort_trajs_by_day_night 
    task: sort_values 
    partial:
      column_name: is_night 
      na_position: first 
      ascending: true
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.split_traj_by_group.return }}
  
  - name: Apply color to trajectories by day/night 
    id: apply_day_night_colormap
    task: apply_color_map 
    partial:
      colormap: 
        - '#e7a553'  # Day 
        - '#292965'  # Night
      input_column_name: is_night
      output_column_name: day_night_colors
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.sort_trajs_by_day_night.return }}

  - name: Exclude unnecessary columns from day/night gdf 
    id: filter_day_night_cols 
    task: subset_columns 
    partial: 
      columns: 
        - dist_meters
        - is_night
        - day_night_colors
        - geometry
      exclude: null
      strict: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.apply_day_night_colormap.return }}

  - name: Create day/night map layers 
    id: generate_day_night_layers 
    task: ecoscope_workflows_ext_custom.tasks.results.create_path_layer
    partial: 
      data_url: null 
      layer_style:
        get_color: day_night_colors
        get_width: 2.25
        width_scale: 1 
        width_min_pixels: 2 
        width_max_pixels: 8 
        width_units: pixels
        cap_rounded: true 
        joint_rounded: true
        billboard: false
        opacity: 0.45
        stroked: true 
      legend:
        title: Day Night tracks
        values: 
          - label: Day 
            color: "#e7a553"
          - label: Night 
            color: "#292965"

    mapvalues:
      argnames: geodataframe
      argvalues: ${{ workflow.filter_day_night_cols.return }}

  - name: Combine overlay layers and day/night layers
    id: combined_ldx_daynight_layers
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers: ${{ workflow.map_overlay.return }}
    mapvalues:
      argnames: grouped_layers
      argvalues: ${{ workflow.generate_day_night_layers.return }}

  - name: Combine day night layers with view state
    id: zip_day_night_with_viewstate
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.combined_ldx_daynight_layers.return }}
        - ${{ workflow.gdf_image_extent.return }}

  - name: Draw day/night map
    id: draw_day_night_map 
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map 
    partial: 
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null 
      max_zoom: 10
      legend_style: 
        placement: bottom-right
    mapvalues:
      argnames: 
        - geo_layers 
        - view_state
      argvalues: ${{ workflow.zip_day_night_with_viewstate.return }}

  - name: Zip day/night filename with text
    id: day_night_text 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.dn_filename.return }}
        - ${{ workflow.draw_day_night_map.return }}

  - name: Persist day/night html
    id: persist_day_night_html
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: null
    mapvalues:
      argnames: 
        - filename
        - text 
      argvalues: ${{ workflow.day_night_text.return }}
    
  - name: Create day night widgets 
    id: create_day_night_widgets 
    task: create_map_widget_single_view 
    skipif:
      conditions:
        - never
    partial: 
      title: Day Night Tracks 
    map: 
      argnames: 
        - view 
        - data 
      argvalues: ${{ workflow.persist_day_night_html.return }}

  - name: Merge day night widgets 
    id: merge_day_night_widgets 
    task: merge_widget_views 
    partial: 
      widgets: ${{ workflow.create_day_night_widgets.return }}

# 4. Home Range /MCP 
  - name: Generate etd 
    id: generate_etd 
    task: calculate_elliptical_time_density
    partial: 
      auto_scale_or_custom_cell_size: 
        auto_scale_or_customize: Auto-scale      
      crs: ESRI:53042
      percentiles: 
        - 50.0 
        - 60.0 
        - 70.0
        - 80.0
        - 90.0
        - 95.0
        - 99.9
      nodata_value: nan 
      band_count: 1
      max_speed_factor: 1.05 
      expansion_factor: 1.3
    mapvalues:
      argnames: trajectory_gdf 
      argvalues: ${{ workflow.split_traj_by_group.return }}

  - name: Reproject etd to wgs84 
    id: reproject_etd 
    task: reproject_gdf 
    partial: 
      target_crs: EPSG:4326
    mapvalues: 
      argnames: gdf 
      argvalues: ${{ workflow.generate_etd.return }}

  - name: zip etd filename with df 
    id: etd_df
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.etd_filename.return }}
        - ${{ workflow.reproject_etd.return }}

  - name: Persist etd gdf
    id: persist_etd_gdf
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: geoparquet
    mapvalues:
      argnames: 
        - filename
        - df
      argvalues: ${{ workflow.etd_df.return }}

  - name: Determine seasonal windows 
    id: determine_seasonal_windows 
    task: ecoscope_workflows_ext_ste.tasks.io.compute_seasons_from_ndvi
    partial: 
      client: ${{ workflow.gee_project_name.return }}
      time_range: ${{ workflow.time_range.return }}
      img_collection: "MODIS/061/MCD43A4"
      nir_band: "Nadir_Reflectance_Band2"
      red_band: "Nadir_Reflectance_Band1"
      chunk_count: 5
    mapvalues: 
      argnames: roi 
      argvalues: ${{ workflow.generate_etd.return }}

  - name: Zip seasons with trajectories 
    id: zip_etd_with_traj 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.determine_seasonal_windows.return }}
        - ${{ workflow.split_traj_by_group.return }}

  - name: Generate season labels 
    id: add_season_labels 
    task: create_seasonal_labels 
    mapvalues: 
      argnames: 
        - seasons_df 
        - trajectories 
      argvalues: ${{ workflow.zip_etd_with_traj.return }}
  
  - name: Generate MCP gdf from trajectories 
    id: generate_mcp 
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.compute_minimum_convex_polygon
    partial: 
      planar_crs: ESRI:53042
    mapvalues: 
      argnames: gdf 
      argvalues: ${{ workflow.split_traj_by_group.return }}

  - name: Reproject mcp gdf to wgs84 
    id: reproject_mcp 
    task: reproject_gdf 
    partial: 
      target_crs: EPSG:4326
    mapvalues: 
      argnames: gdf 
      argvalues: ${{ workflow.generate_mcp.return }}

  - name: Zip mcp filename with gdf 
    id: mcp_gdf 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.mcp_filename.return }}
        - ${{ workflow.generate_mcp.return }}

  - name: Persist mcp gdf
    id: persist_mcp_gdf
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: geoparquet
    mapvalues:
      argnames: 
        - filename
        - df
      argvalues: ${{ workflow.mcp_gdf.return }}

  - name: Apply colormap to home range percentiles 
    id: apply_etd_colormap 
    task: apply_color_map
    partial: 
      input_column_name: percentile
      output_column_name: etd_percentile_colors
      colormap: RdYlGn
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.reproject_etd.return }}

  - name: Exclude unnecessary columns from etd gdf  
    id: filter_etd_cols 
    task: subset_columns 
    partial: 
      columns: 
        - etd_percentile_colors
        - percentile
        - area_sqkm
        - geometry
      exclude: null
      strict: false
    mapvalues: 
      argnames: df
      argvalues: ${{ workflow.apply_etd_colormap.return }}

  - name: Create home range layers 
    id: generate_home_range_layers 
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial: 
      data_url: null
      layer_style: 
        filled: true 
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: etd_percentile_colors
        get_line_color: etd_percentile_colors
        opacity: 0.45 
        get_line_width: 1.55
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5

      legend: 
        title: Home Range Percentiles
        label_column: percentile
        color_column: etd_percentile_colors
        sort: ascending 
        label_suffix: null
    mapvalues: 
      argnames: geodataframe
      argvalues: ${{ workflow.filter_etd_cols.return }}

  - name: Exclude unnecessary columns from mcp gdf  
    id: filter_mcp_cols 
    task: subset_columns 
    partial: 
      columns: 
        - area_km2
        - geometry
      exclude: null
      strict: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.reproject_mcp.return }}

  - name: Create mcp polygon layer 
    id: create_mcp_polygon_layer 
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer 
    partial: 
      data_url: null
      layer_style: 
        filled: false  
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: [255, 20, 147, 50]
        get_line_color: [255, 20, 147, 200]
        opacity: 0.45
        get_line_width: 2.25
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5

      legend: 
          title: Minimum Convex Polygon
          values:
            - label: MCP 
              color: "#ff1493"
    mapvalues: 
      argnames: geodataframe
      argvalues: ${{ workflow.filter_mcp_cols.return }}

  - name: Combine home range layer and mcp layer 
    id: zip_home_range_with_mcp_layer
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.create_mcp_polygon_layer.return }}
        - ${{ workflow.generate_home_range_layers.return }}

  - name: Combine overlay layers and home range layers
    id: combined_ldx_home_range_layers
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers: ${{ workflow.map_overlay.return }}
    mapvalues:
      argnames: grouped_layers
      argvalues: ${{ workflow.zip_home_range_with_mcp_layer.return }}

  - name: Combine home range layers with view state
    id: zip_hr_with_viewstate
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.combined_ldx_home_range_layers.return }}
        - ${{ workflow.gdf_image_extent.return }}

  - name: Draw home range map
    id: draw_home_range_map
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map 
    partial: 
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null 
      max_zoom: 10
      legend_style: 
        placement: bottom-right
    mapvalues:
      argnames: 
        - geo_layers 
        - view_state
      argvalues: ${{ workflow.zip_hr_with_viewstate.return }}

  - name: Zip homerange filename with text 
    id: hr_text_file
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.hr_filename.return }}
        - ${{ workflow.draw_home_range_map.return }}

  - name: Persist homerange html
    id: persist_homerange_html
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: null
    mapvalues:
      argnames: 
        - filename
        - text 
      argvalues: ${{ workflow.hr_text_file.return }}
    
  - name: Create homerange widgets 
    id: create_home_range_widgets 
    task: create_map_widget_single_view 
    skipif:
      conditions:
        - never
    partial: 
      title: Home Range 
    map: 
      argnames: 
        - view 
        - data 
      argvalues: ${{ workflow.persist_homerange_html.return }}

  - name: Merge homerange widgets 
    id: merge_homerange_widgets 
    task: merge_widget_views 
    partial: 
      widgets: ${{ workflow.create_home_range_widgets.return }}

# 5. Mean Speed Raster
  - name: Zip mean speed raster filename with gdf
    id: zip_raster_filename_gdf
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.mean_speed_raster_filename.return }}
        - ${{ workflow.split_traj_by_group.return }}

  - title: Mean Speed Raster
    type: task-group
    description: Configure how the mean speed raster is generated from movement trajectories.
    tasks:
      - name: null
        id: generate_mean_speed_raster
        task: ecoscope_workflows_ext_ste.tasks.results.generate_ecograph_raster
        partial:
          #step_length: 2000
          dist_col: dist_meters
          interpolation: mean
          movement_covariate: speed
          radius: 2
          cutoff: null
          tortuosity_length: 3
          resolution: null
          network_metric: null
          output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
        mapvalues:
          argnames:
            - filename
            - gdf
          argvalues: ${{ workflow.zip_raster_filename_gdf.return }}

  - name: Extract features from mean speed raster 
    id: extract_speed_rasters 
    task: ecoscope_workflows_ext_ste.tasks.io.raster_to_gdf
    mapvalues: 
      argnames: file_path 
      argvalues: ${{ workflow.generate_mean_speed_raster.return }}

  - name: Sort speed features by value 
    id: sort_speed_features_by_value
    task: sort_values 
    partial: 
      column_name: value
      na_position: last 
      ascending: true 
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.extract_speed_rasters.return }}

  - name: Reproject mean speed raster features to wgs84 
    id: reproject_speed_raster 
    task: reproject_gdf 
    partial: 
      target_crs: EPSG:4326
    mapvalues:
      argnames: gdf 
      argvalues: ${{ workflow.sort_speed_features_by_value.return }}

  - name: Apply classification to speed raster features 
    id: apply_classification_raster 
    task: apply_classification
    partial: 
      input_column_name: value
      output_column_name: value_bins 
      classification_options: 
        scheme: natural_breaks
        k: 6
      label_options:
        label_range: false 
        label_decimals: 1
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.reproject_speed_raster.return }}
    
  - name: Apply colormap to speed raster bins 
    id: apply_speed_raster_colormap 
    task: apply_color_map 
    partial: 
      input_column_name: value_bins
      output_column_name: speedraster_bins_colormap 
      colormap: 
        - '#1a9850'
        - '#91cf60'
        - '#d9ef8b'
        - '#fee08b'
        - '#fc8d59'
        - '#d73027'
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.apply_classification_raster.return }}

  - name: Format speed raster bin labels
    id: format_speed_raster_labels
    task: map_values_with_unit
    partial:
      input_column_name: value_bins
      output_column_name: bins_formatted
      original_unit: km/h
      new_unit: km/h
      decimal_places: 1
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.apply_speed_raster_colormap.return }}

  - name: Exclude unnecessary columns from mean speed raster gdf
    id: filter_mean_speed_cols 
    task: subset_columns 
    partial: 
      columns: 
        - value_bins
        - bins_formatted
        - speedraster_bins_colormap
        - geometry
      exclude: null
      strict: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.format_speed_raster_labels.return }}

  - name: Create mean speed raster layer
    id: create_mean_speed_raster_layer 
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer 
    partial: 
      data_url: null
      layer_style: 
        filled: true 
        stroked: false 
        extruded: false 
        wireframe: false
        get_fill_color: speedraster_bins_colormap
        get_line_color: speedraster_bins_colormap
        opacity: 0.45
        get_line_width: 1.55
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5

      legend: 
        title: Mean Speed Raster (km/h)
        label_column: bins_formatted
        color_column: speedraster_bins_colormap
        sort: ascending 
        label_suffix: null
    mapvalues: 
      argnames: geodataframe
      argvalues: ${{ workflow.filter_mean_speed_cols.return }}

  - name: Combine overlay layers and mean speed raster layer
    id: combined_ldx_speed_raster
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers: ${{ workflow.map_overlay.return }}
    mapvalues:
      argnames: grouped_layers
      argvalues: ${{ workflow.create_mean_speed_raster_layer.return }}

  - name: Combine mean speed raster layer with view state
    id: zip_speed_raster_viewstate
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.combined_ldx_speed_raster.return }}
        - ${{ workflow.gdf_image_extent.return }}

  - name: Draw mean speed raster map
    id: draw_mean_speed_raster_map
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map 
    partial: 
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null 
      max_zoom: 10
      legend_style: 
        placement: bottom-right
    mapvalues:
      argnames: 
        - geo_layers 
        - view_state
      argvalues: ${{ workflow.zip_speed_raster_viewstate.return }}

  - name: Zip mean speed raster filename with text 
    id: mean_speed_text 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.mr_filename.return }}
        - ${{ workflow.draw_mean_speed_raster_map.return }}

  - name: Persist mean speed raster html
    id: persist_mean_speed_raster_html
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: null
    mapvalues:
      argnames: 
        - filename
        - text 
      argvalues: ${{ workflow.mean_speed_text.return }}

  - name: Create mean speed raster widgets 
    id: create_mean_speed_raster_widgets 
    task: create_map_widget_single_view 
    skipif:
      conditions:
        - never
    partial: 
      title: Mean Speed Raster 
    map: 
      argnames: 
        - view 
        - data    
      argvalues: ${{ workflow.persist_mean_speed_raster_html.return }}

  - name: Merge mean speed raster widgets
    id: merge_mean_speed_raster_widgets 
    task: merge_widget_views 
    partial: 
      widgets: ${{ workflow.create_mean_speed_raster_widgets.return }}

  # 6. Seasons Home Range Map
  - name: Calculate seasonal home range 
    id: seasonal_home_range 
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.calculate_elliptical_time_density_grouped
    partial:
      groupby_cols: 
        - season 
      percentiles: 
      - 99.9 
      cell_size: 
        auto_scale_or_custom: Auto-scale #Customize 
      drop_null_groups: true
    mapvalues:
      argnames: gdf
      argvalues: ${{ workflow.add_season_labels.return }}

  - name: Convert season column to string 
    id: convert_season_to_string 
    task: convert_columns_to_string
    partial: 
      columns: 
        - season 
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.seasonal_home_range.return }}

  - name: Zip seasonal etd filename with gdf
    id: seasonal_etd_gdf
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.seasonal_etd_filename.return }}
        - ${{ workflow.convert_season_to_string.return }}

  - name: Persist seasonal etd gdf
    id: persist_seasonal_etd_gdf
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: geoparquet
    mapvalues:
      argnames: 
        - filename
        - df
      argvalues: ${{ workflow.seasonal_etd_gdf.return }}

  - name: Reproject seasonal home range gdf to wgs84 
    id: reproject_seasonal_home_range 
    task: reproject_gdf 
    partial: 
      target_crs: EPSG:4326
    mapvalues: 
      argnames: gdf 
      argvalues: ${{ workflow.convert_season_to_string.return }}

  - name: Sort seasonal homerange by seasons
    id: sort_seasons
    task: sort_values 
    partial:
      column_name: season
      na_position: first 
      ascending: true
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.reproject_seasonal_home_range.return }}

  - name: Map column values to assign colors to seasons 
    id: assign_season_colors 
    task: add_mapped_column_value 
    partial: 
      column: season 
      new_column: season_hex 
      default: null 
      keep_unmapped: false 
      mapping: {
        "Dry" : "#f57c00",
        "Wet" : "#255084" 
      }
    mapvalues: 
      argnames: df 
      argvalues: ${{ workflow.sort_seasons.return }}
  
  - name: Apply seasonal hex to rgba 
    id: seasonal_hex_rgba
    task: add_rgba_from_hex 
    partial: 
      column: season_hex 
      new_column: season_colors 
    mapvalues: 
      argnames: df 
      argvalues: ${{ workflow.assign_season_colors.return }}

  # - name: Assign season colors to dataframe 
  #   id: assign_season_df
  #   task: apply_color_map 
  #   partial:
  #     colormap: 
  #       - "#f57c00"  # Dry 
  #       - "#255084"  # Wet
  #     input_column_name: season
  #     output_column_name: season_colors
  #   mapvalues:
  #     argnames: df
  #     argvalues: ${{ workflow.sort_seasons.return }} 

  - name: Exclude unnecessary columns from mean speed raster gdf
    id: filter_season_cols 
    task: subset_columns 
    partial: 
      columns: 
        - season_colors
        - season
        - geometry
      exclude: null
      strict: false
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.seasonal_hex_rgba.return }}

  - name: Create seasonal home range layers
    id: generate_season_layers 
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial: 
      data_url: null
      layer_style: 
        filled: true 
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: season_colors
        get_line_color: season_colors
        opacity: 0.45
        get_line_width: 0.35
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5

      legend: 
          title: Seasonal Home Range
          label_column: season
          color_column: season_colors
          sort: ascending 
          label_suffix: null
    mapvalues: 
      argnames: geodataframe
      argvalues: ${{ workflow.filter_season_cols.return }}

  - name: Combine overlay layers and seasonal home range layers
    id: combined_ldx_seasonal_hr_layers
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers: ${{ workflow.map_overlay.return }}
    mapvalues:
      argnames: grouped_layers
      argvalues: ${{ workflow.generate_season_layers.return }}

  - name: Combine seasonal home range layers with view state
    id: zip_seasonal_hr_with_viewstate
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.combined_ldx_seasonal_hr_layers.return }}
        - ${{ workflow.gdf_image_extent.return }}

  - name: Draw seasonal home range map
    id: draw_seasonal_home_range_map
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map 
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null 
      max_zoom: 10
      legend_style: 
        placement: bottom-right
    mapvalues:
      argnames: 
        - geo_layers 
        - view_state
      argvalues: ${{ workflow.zip_seasonal_hr_with_viewstate.return }}

  - name: Zip seasonal homerange filename with text 
    id: seasonal_hr_text 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial:
      iterables:
        - ${{ workflow.seasonal_hr_filename.return }}
        -  ${{ workflow.draw_seasonal_home_range_map.return }}

  - name: Persist seasonal home range html
    id: persist_seasonal_home_range_html
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: null
    mapvalues:
      argnames: 
        - filename
        - text 
      argvalues: ${{ workflow.seasonal_hr_text.return }}

  - name: Create seasonal home range widgets
    id: create_seasonal_hr_widgets
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: Seasonal Home Range
    map:
      argnames: 
        - view 
        - data    
      argvalues: ${{ workflow.persist_seasonal_home_range_html.return }}

  - name: Merge seasonal home range widgets
    id: merge_seasonal_hr_widgets
    task: merge_widget_views
    partial:
      widgets: ${{ workflow.create_seasonal_hr_widgets.return }}

  # 7. Summary metrics 
  - name: Calculate total mcp area
    id: total_mcp_area
    task: dataframe_column_sum
    partial:
      column_name: area_km2
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.generate_mcp.return }}

  - name: Convert total mcp area to quantity
    id: coverage_mcp_quantity
    task: to_quantity
    partial:
      unit: "km²"
    mapvalues:
      argnames: value
      argvalues: ${{ workflow.total_mcp_area.return }}

  - name: Create single value widgets for total MCP area per group
    id: total_mcp_sv_widgets
    task: create_single_value_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: Total MCP Area
      decimal_places: 2
    map:
      argnames:
        - view
        - data
      argvalues: ${{ workflow.coverage_mcp_quantity.return }}

  - name: Merge per group total MCP area SV widgets
    id: total_mcp_grouped_sv_widget
    task: merge_widget_views
    partial:
      widgets: ${{ workflow.total_mcp_sv_widgets.return }}

  - name: Calculate total grid area
    id: total_grid_area
    task: dataframe_column_sum
    partial:
      column_name: area_sqkm
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.generate_etd.return }}

  - name: Convert total grid area to quantity
    id: coverage_grid_quantity
    task: to_quantity
    partial:
      unit: "km²"
    mapvalues:
      argnames: value
      argvalues: ${{ workflow.total_grid_area.return }}

  - name: Create single value widgets for total grid area per group
    id: total_grid_sv_widgets
    task: create_single_value_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: Total Grid Area
      decimal_places: 2
    map:
      argnames:
        - view
        - data
      argvalues: ${{ workflow.coverage_grid_quantity.return }}

  - name: Merge per group total grid area SV widgets
    id: total_grid_grouped_sv_widget
    task: merge_widget_views
    partial:
      widgets: ${{ workflow.total_grid_sv_widgets.return }}

  - name: Capitalize subject gender 
    id: capitalize_subject
    task: format_text_column 
    partial: 
      column: subject_sex 
      method: capitalize
    mapvalues:
      argnames: df 
      argvalues: ${{ workflow.split_traj_by_group.return }}

  - name: Determine subject gender 
    id: subject_gender
    task: column_first_unique_value
    partial:
      column_name: subject_sex
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.capitalize_subject.return }}

  - name: Create Single Value Widgets for subject gender per group
    id: gender_widgets
    task: create_text_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: Gender
    map:
      argnames:
        - view
        - data
      argvalues: ${{ workflow.subject_gender.return }}

  - name: Merge per group gender SV widgets
    id: gender_sv_widget
    task: merge_widget_views
    partial:
      widgets: ${{ workflow.gender_widgets.return }}

  - name: Report duration in months
    id: report_duration
    task: get_duration
    partial:
      time_range: ${{ workflow.time_range.return }}
      time_unit: months

  - name: Round off report duration to 2 decimal_places
    id: round_report_duration
    task: round_off_values
    partial:
      dp: 2
      value: ${{ workflow.report_duration.return }}

  - name: Get unique subject names from trajectories
    id: unique_subjects
    task: dataframe_column_nunique
    partial:
      df: ${{ workflow.rename_traj_cols.return }}
      column_name: subject_name

  - title: Generate Word Doc Report
    type: task-group
    description: Configure word report settings
    tasks:
      - name: null
        id: include_maps_toggle
        task: skip_toggle
        partial: 
          enabled: True 

      - name: null
        id: logo_path
        task: get_file_path
        partial:
          output_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}

  - name: Skip movement tracks html
    id: skip_movement_tracks_html
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
    mapvalues:
      argnames: filename
      argvalues: ${{ workflow.persist_movement_tracks_html.return }}

  - name: Skip speedmap html
    id: skip_speedmap_html
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
    mapvalues:
      argnames: filename
      argvalues: ${{ workflow.persist_speedmap_html.return }}

  - name: Skip day/night html
    id: skip_day_night_html
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
    mapvalues:
      argnames: filename
      argvalues: ${{ workflow.persist_day_night_html.return }}

  - name: Skip homerange html
    id: skip_homerange_html
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
    mapvalues:
      argnames: filename
      argvalues: ${{ workflow.persist_homerange_html.return }}

  - name: Skip mean speed raster html
    id: skip_mean_speed_raster_html
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
    mapvalues:
      argnames: filename
      argvalues: ${{ workflow.persist_mean_speed_raster_html.return }}

  - name: Skip seasonal home range html
    id: skip_seasonal_home_range_html
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
    mapvalues:
      argnames: filename
      argvalues: ${{ workflow.persist_seasonal_home_range_html.return }}

  - name: Generate movement tracks html to png
    id: movement_png
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      config: 
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    mapvalues: 
      argnames: html_path 
      argvalues: ${{ workflow.skip_movement_tracks_html.return }}

  - name: Generate speedmap html to png
    id: speedmap_png
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      config: 
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    mapvalues: 
      argnames: html_path 
      argvalues: ${{ workflow.skip_speedmap_html.return }}

  - name: Generate day/night html to png
    id: day_night_png
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      config: 
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    mapvalues: 
      argnames: html_path 
      argvalues: ${{ workflow.skip_day_night_html.return }}

  - name: Generate homerange html to png
    id: homerange_png
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      config: 
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    mapvalues: 
      argnames: html_path 
      argvalues: ${{ workflow.skip_homerange_html.return }}

  - name: Generate mean speed raster html to png
    id: mean_speed_raster_png
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      config: 
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    mapvalues: 
      argnames: html_path 
      argvalues: ${{ workflow.skip_mean_speed_raster_html.return }}

  - name: Generate seasonal home range html to png
    id: seasonal_hr_png
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      config: 
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    mapvalues: 
      argnames: html_path 
      argvalues: ${{ workflow.skip_seasonal_home_range_html.return }}

  - name: Download mapbook cover page templates
    id: download_mapbook_cover_page
    task: ecoscope_workflows_ext_ste.tasks.io.fetch_and_persist_file
    partial:
      url: https://www.dropbox.com/scl/fi/v2rbwywas1ag645qjptsk/mapbook_cover_page.docx?rlkey=pmpmkgjpjoc4i8ngt7qfipstz&st=xqplosdg&dl=0
      output_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      overwrite_existing: false
      unzip: false
      retries: 2

  # Download section templates
  - name: Download mapbook section templates
    id: download_sect_templates
    task: ecoscope_workflows_ext_ste.tasks.io.fetch_and_persist_file
    partial:
      url: https://www.dropbox.com/scl/fi/2h1gakoqgtsxbw2axibmk/mapbook_grouper_template.docx?rlkey=3jz96yxacnotzrbx32v06jvj1&st=5kc9g34a&dl=0
      output_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      overwrite_existing: false
      unzip: false
      retries: 2

  - name: Skip logo path for cover page generation
    id: skip_logo_path
    task: skip_file
    partial:
      enabled: ${{ workflow.include_maps_toggle.return }}
      filename: ${{ workflow.logo_path.return }}

  - name: Create cover page context 
    id: cover_page_context 
    task: prepare_cover_metadata 
    partial: 
      org_logo_path: ${{ workflow.skip_logo_path.return }}
      report_period: ${{ workflow.time_range.return }}
      prepared_by: "Ecoscope"
      extra_fields:
        subject_count: ${{ workflow.unique_subjects.return }}
      time_generated_format: "%Y-%m-%d %H:%M:%S"

  - name: Gate cover page filename on word doc toggle
    id: gate_cover_page_filename
    task: skip_file
    partial:
      filename: cover_page.docx
      enabled: ${{ workflow.include_maps_toggle.return }}

  - name: Persist cover page
    id: persist_cover_page
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: create_context_page
    partial:
      template_path: ${{ workflow.download_mapbook_cover_page.return }}
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      context: ${{ workflow.cover_page_context.return }}
      filename: ${{ workflow.gate_cover_page_filename.return }}

  - name: Group mapbook context values 
    id: group_mapbook_ctx 
    skipif:
      conditions:
        - any_dependency_skipped
        - any_keyed_iterables_are_skips
      unpack_depth: 1
    task: groupbykey
    partial: 
      iterables: 
        - ${{ workflow.split_traj_by_group.return }} #df
        - ${{ workflow.coverage_grid_quantity.return }} # grid area
        - ${{ workflow.coverage_mcp_quantity.return }} # mcp area 

  - name: Create mapbook context 
    id: build_mapbook_context 
    task: create_mapbook_context 
    partial: 
      current_period: ${{ workflow.time_range.return }}
      previous_period: ${{ workflow.set_previous_period.return }}
      period: ${{ workflow.round_report_duration.return }}
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
    mapvalues:
      argnames: 
        - df
        - grid_area 
        - mcp_area 
      argvalues: ${{ workflow.group_mapbook_ctx.return }}

  - name: Gate mapbook page filename on word doc toggle
    id: gate_mapbook_page_filename
    task: skip_file
    partial:
      filename: ""
      enabled: ${{ workflow.include_maps_toggle.return }}

  - name: Persist individual mapbook page
    id: persist_mapbook_page
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: render_mapbook_page
    partial:
      template_path: ${{ workflow.download_sect_templates.return }}
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename: ${{ workflow.gate_mapbook_page_filename.return }}
      strict_images: true
      box_h_cm: 6.5
      box_w_cm: 11.11
    mapvalues:
      argnames: context
      argvalues: ${{ workflow.build_mapbook_context.return }}

  - name: Merge cover page and individual mapbook documents
    id: merge_mapbook_docs
    skipif:
      conditions:
        - any_dependency_skipped
      unpack_depth: 1
    task: merge_docx_documents
    partial:
      cover_page_path: ${{ workflow.persist_cover_page.return }}
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      order_by: name
      filename: overall_report.docx
      context_page_items: ${{ workflow.persist_mapbook_page.return }}

  - name: Mapbook dashboard 
    id: mapbook_dashboard 
    task: gather_dashboard 
    partial: 
      details: ${{ workflow.workflow_details.return }}
      widgets: 
        - ${{ workflow.gender_sv_widget.return }} # gender str widget
        - ${{ workflow.total_mcp_grouped_sv_widget.return }} # total mcp area widget
        - ${{ workflow.total_grid_grouped_sv_widget.return }} # total grid area widget

        - ${{ workflow.merge_movement_tracks_widgets.return }} # movement tracks 
        - ${{ workflow.merge_homerange_widgets.return }} # home range 
        - ${{ workflow.merge_speedmap_widgets.return }} # speedmap 
        - ${{ workflow.merge_mean_speed_raster_widgets.return }} # speedraster widgets 
        - ${{ workflow.merge_day_night_widgets.return }} # day/night widgets 
        - ${{ workflow.merge_seasonal_hr_widgets.return }} #seasons 
      
      time_range: ${{ workflow.time_range.return }}
      groupers: ${{ workflow.groupers.return }}